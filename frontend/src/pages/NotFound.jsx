import React from 'react';
import NotFound from '../components/NotFound';

/**
 * NotFound page component that displays a 404 error message.
 */
const NotFoundPage = () => {
    return <NotFound />;
};

export default NotFoundPage;
